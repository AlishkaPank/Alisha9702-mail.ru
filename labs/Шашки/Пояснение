Команды для шашки.
В классе cheker определены 4 команды: 
шашка ходит вправо по диагонали, её ход будет увеличивать  x и  y на 1:  ++x,++y. 
шашка ходит влево по диагонали, её ход будет увеличивать  y на 1 и уменьшать  x на 1 :  --x,++y.
шашка бьёт по правой  диагонали: x+2 , y +2.
шашка бьёт по правой  диагонали: x-2 , y +2.

Для хранения последовательности команд применяется двусвязный список, благодаря этому мы можем сделать ход назад. 

С помощью HashMap мы создаём набор команд, в котором элементы  будут храниться в формате (название команды-команда)

В move определяюся методы register, execute, undo.

Register получает данные о команде, если формат верный, то добавляет в список commands.

Execute используется  для реализации команд  из класса cheker и заносит их в список undoCommands.

А undo используется  для реализации отмены команд . После каждой реализации удаляет команду из своего списка.
