У нас  есть два людоеда, т.е. два класса OgreFirst и OgreSecond, у которых есть общие методы и 
свои  личные, которые их отличают друг от друга. Пусть hunt()-пойман*, eat()-съеден* и  sleep()-спит* будут общими методами,
и будут вызываться для каждого, а методом speak() будет обладать только класс OgreSecond, 
а метод boil() будет определяться у каждого класса, но с разными действиями, и поэтому будет абстрактным методом. 
Для класса OgreFirst будет определяться как “пожарил”, а для класса OgreSecond будет определять “варит” или “жарит”.

Шаблонный метод  задан в базовом классе, он состоит из вызовов методов  в определённом порядке.

Сначала выполняются методы для первого людоеда. Общий метод hunt()-пойман(человек пойман на охоте)будет выполнен; 
метод speak() будет пропущен, потому что относится ко второму огру; общий метод eat()-съеден,  будет выполнен;
общий метод sleep()-выполнен; метод-шаблон boil() – будет выбран из класса OgreFirst – “пожарил”. 

Для второга огра будут выполнены: общий метод hunt()-пойман; метод speak() будет выполнен, для класса OgreSecond он определён;
общий метод eat()-съеден,  будет выполнен; общий метод sleep()-выполнен; 
метод-шаблон boil() – будет выбран из класса OgreSecond, в main для OgreSecond мы определили  день недели (Week.WeekEnd), 
а в классе OgreSecond  поставили условие ,что в рабочий день он будет варить, а в выходной день – жарить. 
Поэтому будет выведено “Жарит”.
